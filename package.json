{
  "name": "scrum-poker",
  "version": "0.1.2",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^3.9.3",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn build && cd build && now -n scrum-poker && cd .."
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "now": "^16.6.0",
    "react-test-renderer": "^16.12.0"
  }
}
